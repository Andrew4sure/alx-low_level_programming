#include <stdio.h>
/*
 * Main description: A program that prints the alphabet in lowercase
 *
 * Return: Always 0 (succcess)
 *
 */
int main
{
	while (char = a)
	{
		putchar("a\n" "%c", char);
	a = a++
	}
	return (0);
}
