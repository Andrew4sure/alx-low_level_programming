#include "main.h"
/**
 * main - Entry point
 *
 * Return: always 0 (success)
 */
/*
 * print_alphabet - prints the alphabet in lowercase
 */
int main(void)
void print_alphabet(void)
{
	int i;
	char i;

	for (i = a; i <= z; i++)
	{
		putchar(i);
	}
	purchar("\n");
}
