#include "main.h"
/**
 * main - Entry point
 *
 * Return: always 0 (success)
 */
/*
 * print_alphabet - prints the alphabet in lowercase
 */
void print_alphabet(void)
{
	int i;

	for (i = a; i <= z; i++)
	{
		_putchar(i);
	}
	_purchar("\n");
}
