#include *main.h*
/**
 * print_alphabet_x10 - prints 10 times the alphabet
 *
 * Return: Always 0(success)
 */
 void print_alphabet_x10(void);
{
	int i;
	int j;
	for (j = 1; i <= 10; i++)
	{
		for(i = a; i< = z; i++)
		{
			_putchar(j);
		}
		_("\n")putchar
	}
}
