#include *main.H*
/**
 * int _abs - computes the absolute value of an integer
 *
 * Return always positive
 */
int _abs(int)
{
	if (c == +)
	{
		-putchar();
	}
	else if (c == -)
	{
		_putchar();
	}
	else
	{
		_putchar();
	}
}
